#https://cloud.google.com/cloud-build/docs/deploying-builds/deploy-firebase
steps:
  - name: 'gcr.io/cloud-builders/git'
    entrypoint: 'bash'
    args:
    - '-c'
    - |
      set -e
      # Fetch the societySidekick-common submodule from its public GitHub URL (in .gitmodules).
      # `submodule sync` resets any cached/rewritten URL (e.g. an old Cloud Source mirror) back to
      # the .gitmodules value before fetching.
      git submodule sync --recursive
      git submodule update --init --recursive
      echo "src/common..."
      ls -a ./src/common
      # Fail fast with a clear message if the submodule did not populate.
      test -f ./src/common/constants.js || { echo "ERROR: src/common submodule did not populate"; exit 1; }
  # Firebase
#   - name: 'gcr.io/cloud-builders/git'
#     entrypoint: 'bash'
#     args:
#     - '-c'
#     - |
#       echo "one up..."
#       ls -a ../
#       echo "current..."
#       ls -a
#       echo "src..."
#       ls -a ./src
#       echo "src/common..."
#       ls -a ./src/common
#   - name: 'docker:stable'
#     args: [ 'build', '-t', 'gcr.io/$PROJECT_ID/firebase', '.' ]
#   - name: 'node:lts'
#     entrypoint: 'npm'
#     args: ['install', '-g', 'firebase-tools']
  # Install
#   - name: 'gcr.io/cloud-builders/git'
#     entrypoint: 'bash'
#     args:
#     - '-c'
#     - |
#       echo "one up..."
#       ls -a ../
#       echo "current..."
#       ls -a
#       echo "src..."
#       ls -a ./src
#       echo "src/common..."
#       ls -a ./src/common
  #- name: 'gcr.io/cloud-builders/npm:current'
  - name: 'node:lts'
    entrypoint: 'npm'
    args: ['install']
  # Test
  #- name: 'gcr.io/cloud-builders/npm:current'
  #- name: 'node:lts'
  # entrypoint: 'npm'
  #  args: ['run', 'test:unit']
  # Build
#   - name: 'gcr.io/cloud-builders/git'
#     entrypoint: 'bash'
#     args:
#     - '-c'
#     - |
#       echo "one up..."
#       ls -a ../
#       echo "current..."
#       ls -a
#       echo "src..."
#       ls -a ./src
#       echo "src/common..."
#       ls -a ./src/common
  #- name: 'gcr.io/cloud-builders/npm:current'
  - name: 'node:lts'
    entrypoint: 'npm'
    args: ['run', 'build']
    env:
      - '_CONFIG=$_CONFIG'
#   - name: 'docker:latest'
#     args: [ 'build', '-t', 'gcr.io/$PROJECT_ID/firebase', '.' ]
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
    - --destination=gcr.io/$PROJECT_ID/firebase
    - --cache=true
  # Deploy
  - name: 'gcr.io/$PROJECT_ID/firebase'
    args: [ 'deploy', '--project', '$PROJECT_ID', '--token', '$_FIREBASE_TOKEN' ]
