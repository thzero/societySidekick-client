class BaseGameSystems {
	// eslint-disable-next-line
	async execute(framework, app, router, store) {
		this._initialize(framework);
	}

	// eslint-disable-next-line
	_initialize(Vue) {
	}
}

export default BaseGameSystems;
