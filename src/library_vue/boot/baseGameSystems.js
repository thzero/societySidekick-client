class BaseGameSystems {
	// eslint-disable-next-line
	async execute(Vue, app, router, store) {
		this._initialize(Vue);
	}

	// eslint-disable-next-line
	_initialize(Vue) {
	}
}

export default BaseGameSystems;
